import { render, fireEvent, screen } from '@testing-library/react';
import TopicsProvider from './topics_context';

// Modify this template to test context
describe('YourContext', () => {
    it('Do something', () => {
      // Add more tests here
    });
  });
  